pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/Pranay921/jenkins-repo'
            }
        }

        stage('Build') {
            steps {
                bat 'dir'
            }
        }

    }
}

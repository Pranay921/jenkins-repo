pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/your-username/jenkins-demo.git'
            }
        }

        stage('Build') {
            steps {
                bat 'dir'
            }
        }

    }
}
